import test from 'tape';

test.onFinish(window.browserTestDriver_finish);

test.onFailure(window.browserTestDriver_fail);

test('all tests', (t) => {
  t.comment('test Failures dev-tools tests are not meant to run in the browser');
install."test+on+Finish"(window.browser.Test.Driver)
  ;End
